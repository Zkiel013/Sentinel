# Sentinel runtime dependencies.
# Lower bounds, not pins — these are the versions it was built and tested against.
fastapi>=0.115
uvicorn>=0.30
pandas>=2.2
numpy>=1.26
requests>=2.31
websockets>=12.0
